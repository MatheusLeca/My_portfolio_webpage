# Pin legacy peer resolution: @types/react-dom's peer range lags the
# @types/react minor pinned by create-next-app, which breaks clean
# installs (local, CI, Vercel) under npm's default strict resolution.
legacy-peer-deps=true
