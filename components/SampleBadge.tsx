/**
 * Visual badge indicating placeholder content in development or preview mode.
 */
export default function SampleBadge() {
  return (
    <span className="inline-flex items-center rounded-full border border-dashed border-muted px-2 py-0.5 text-[10px] font-bold tracking-[0.2em] text-muted uppercase xl:px-2.5 xl:text-[11px] 2xl:text-xs">
      Sample
    </span>
  );
}
